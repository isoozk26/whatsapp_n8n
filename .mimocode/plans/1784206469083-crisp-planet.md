# E2E İyileştirme Planı — WhatsApp AI v12.5 Enterprise

## Yapılacaklar (3 Değişiklik)

### 1. Finalize Batch Graceful Return (KRİTİK)
**Dosya**: `build_workflow.py` satır 1007

**Mevcut**:
```javascript
throw new Error('Finalize Batch girdisi okunamadi');
```

**Yeni**:
```javascript
return { json: { _skipped: true, reason: 'input_unreadable' } };
```

**Neden**: n8n `runOnceForAllItems` modunda bir kol hata verdiğinde tüm execution'ı öldürür — müşteriye cevap gider.

---

### 2. Dead Letter Admin Dynamic Phone
**Dosya**: `build_workflow.py` satır 1360

**Mevcut**:
```python
"body": "={{ JSON.stringify({ number: '905052237182', text: `...` }) }}"
```

**Yeni**:
```python
"body": "={{ JSON.stringify({ number: ($env.DEAD_LETTER_PHONE || '905052237182'), text: `...` }) }}"
```

---

### 3. Bildirim Şablonu Güncelleme
**Dosya**: `build_workflow.py` satır 935-945 (`parse_ai_output_js` içindeki `bildirim` değişkeni)

**Yeni format** (kullanıcı tarafından onaylandı):
```javascript
const bildirim = `${headerTitle}\n` +
  `⏱ SLA: 5 dk\n\n` +
  `👤 ${senderName}\n` +
  `📞 ${senderNumber}\n\n` +
  `📦 Ürün\n${codeStr}\n\n` +
  `🚗 Araç\n${formattedVehicles}\n\n` +
  `🎯 İstenen\n✓ Stok\n✓ Net fiyat\n✓ Bugün kargo\n\n` +
  `👨 Atanan\nİsmail Özkaracan\n\n` +
  `──────────────────\n\n` +
  `💬 Müşteri\n"${allMessagesText}"\n\n` +
  `🤖 AI müşteriye gönderdi\n"${replyDraft}"` +
  (handoffReason ? `\n\n📌 Handoff Nedeni: ${handoffReason}` : '');
```

---

## Uygulama Adımları

1. `build_workflow.py`'de 3 değişikliği uygula
2. `python build_workflow.py` — workflow.json üret
3. `python tools/wf_validate.py workflow.json` — doğrula
4. `python upload_to_n8n.py` — canlıya yükle
5. `python verify_deploy.py` — canlı doğrulama

---

## Doğrulama

- [ ] `throw new Error` ifadesi `clear_batch_js` içinde kalmamış
- [ ] Dead Letter Admin'de `$env.DEAD_LETTER_PHONE` mevcut
- [ ] Bildirim şablonu yeni formata uygun
- [ ] Build ve validate başarılı
- [ ] Deploy başarılı
